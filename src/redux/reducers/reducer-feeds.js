const defaultState = {
  loading: false,
  error: null,
  feedList: [],
};

const feedReducer = (state = defaultState, actions) => {
  switch (actions.type) {
    default: return state;
  }
};

export default feedReducer;
